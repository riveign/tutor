# Postgres connection used by the Rust API and any local sqlx tooling.
DATABASE_URL=postgres://tutor:tutor@localhost:55432/tutor

# Socket the Axum server binds to.
API_BIND=0.0.0.0:8080

# Optional. When unset the web app falls back to the Vite proxy at /api.
# VITE_API_URL=http://localhost:8080/api
